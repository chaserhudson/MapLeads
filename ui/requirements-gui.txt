# GUI-specific requirements (in addition to main requirements.txt)
# tkinter is included with Python by default

# Optional enhancements for better GUI experience:
# ttkthemes>=3.2.0  # For better styling themes (optional)
# tkinter-tooltip>=2.0.0  # For enhanced tooltips (optional)

# Core dependencies are already in requirements.txt:
# - sqlite3 (built into Python)
# - threading (built into Python) 
# - subprocess (built into Python)
# - json (built into Python)

# Note: All GUI dependencies are built into Python's standard library
# No additional packages required for basic functionality
